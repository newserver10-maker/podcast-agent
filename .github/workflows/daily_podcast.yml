name: Daily Podcast Agent

on:
  schedule:
    - cron: "0 21 * * *" # UTC 21:00 = KST 06:00
  workflow_dispatch:

jobs:
  run-agent:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          # Install Patchright browsers
          python -m patchright install chromium

      - name: Restore Auth
        env:
          NOTEBOOKLM_AUTH_STATE: ${{ secrets.NOTEBOOKLM_AUTH_STATE }}
        run: python export_auth.py restore

      - name: Verify Auth State
        run: |
          echo "Current Path: $(pwd)"
          echo "Listing data directory:"
          ls -R data || echo "Data directory not found"
          if [ -f "data/browser_state/state.json" ]; then
            echo "✅ state.json exists"
          else
            echo "❌ state.json IS MISSING!"
            exit 1
          fi

      - name: Run Podcast Agent
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: python main.py --now
